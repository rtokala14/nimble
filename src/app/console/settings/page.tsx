import React from "react";

import { type Metadata } from "next";

export const metadata: Metadata = {
  title: "Settings | Nimble",
};

function Page() {
  return <div>Settings</div>;
}

export default Page;
