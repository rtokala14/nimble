import React from "react";

import { type Metadata } from "next";

export const metadata: Metadata = {
  title: "Reminders | Nimble",
};

function Page() {
  return <div>Reminders</div>;
}

export default Page;
